# Asset Slots

When the final files are available, place them here and wire them into `styles.css`.

- `hero.jpg` - top full-width speaking photo.
- `video-still.jpg` - right-side press/speaking image with play button.
- logo files - replace the temporary text badges in `index.html` with image tags while keeping the `logo-grid` layout.
